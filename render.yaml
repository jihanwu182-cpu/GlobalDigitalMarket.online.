services:
  - type: web
    runtime: static
    name: globaldigitalmarket-online
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
